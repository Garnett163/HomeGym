export const typeScriptQuestions = [
  {
    id: 1,
    question: 'Для чего нужен TypeScript?',
    answer: `JS динамическая типизация (типы проверяются в ран тайме), TS-сложная и статическая (на этапе компиляции) + структурная (одинаковые поля).
      Минусы - перед запуском его необходимо транспилировать в JS,  - Увеличение объема кода,  - Не защищает от ошибок в runtime`,
  },
  {
    id: 2,
    question: 'Что такое Generics?',
    answer:
      'Generics — позволяют создавать типы, способные работать с различными типами, а не только с одним строго определенным типом. Т.е. мы определяем типы в момент использования. Пример - ',
  },
  {
    id: 3,
    question: 'Различие между Type и Interface?',
    answer: `Type - должно быть уникальное название и можно типизировать все что угодно, можно указывать литералы и явное значение, подходит для Mapped types.
       Interface - можно делать extends, declaration merging(одинаковые имена), производительность лучше чем у type с пересечением.`,
  },
  {
    id: 4,
    question: 'Asserts и infer что такое?',
    answer: `asserts — это statement, который проверяет булево выражение. Если выражение истинно, выполнение продолжается. Если ложно — выбрасывается ошибка. 
      infer - вытащить параметр или return тип из функции, основа для утилитарных типов`,
  },
  {
    id: 5,
    question: 'TypeGuard?',
    answer:
      'TypeGuard - это функция которая возвращает true или false в зависимости от проверки типа. Имеет специальный тип возврата value is Type (Если эта функция вернула true, считай, что value имеет тип Type)',
  },
  {
    id: 6,
    question: 'Что такое Mapped types?',
    answer:
      'Mapped types позволяет пройтись по всем ключам { [key in keyof T]: T[key] } существующего типа и создать новый тип, трансформируя каждое свойство.',
  },
  {
    id: 7,
    question: 'Enum',
    answer: `Лучше использовать Object + as const и объекта можно получить union его значений: type T = typeof Obj[keyof typeof Obj]`,
  },
  {
    id: 8,
    question: 'Utility types?',
    answer: `Встроенные дженерики TypeScript для трансформации типов. Помогают не писать однотипные типы вручную.
  Для свойств объекта (модификация существующего типа):
  — Partial<T> — все поля опциональны (?).
  — Required<T> — все поля обязательны (убирает ?).
  — Readonly<T> — все поля только для чтения.
  Для конструирования объектов (создание нового типа):
  — Pick<T, K> — берёт только указанные ключи. K extends keyof T.
  — Omit<T, K> — исключает указанные ключи.
  — Record<K, V> — создаёт тип-объект, где ключи K, значения V.
  Для union-типов (работа как с множествами):
  — Exclude<T, U> — убирает из T типы, входящие в U.
  — Extract<T, U> — оставляет из T только входящие в U (противоположность Exclude).
  — NonNullable<T> — убирает null и undefined (по сути Exclude<T, null | undefined>).
  Для функций (извлечение типов через infer):
  — ReturnType<T> — тип возвращаемого значения.
  — Parameters<T> — кортеж типов параметров.
  — Awaited<T> — «разворачивает» Promise (рекурсивно).
  Важно: большинство из них — синтаксический сахар над mapped types и conditional types с infer.
  Например, ReturnType внутри: T extends (...args: any) => infer R ? R : never.
  Pick внутри: { [P in K]: T[P] }.

  В реальной работе часто комбинирую: Partial<Pick<User, 'name' | 'email'>> для форм редактирования.`,
  },
  {
    id: 9,
    question: 'Declaration?',
    answer: `Нужны для типизации библиотек, для описания глобальных переменных / API, 
    @types/* — пакеты с .d.ts для библиотек, у которых нет встроенных типов
    declare не создаёт JavaScript-код. Он сообщает TypeScript о типах сущности, которая существует где-то в runtime`,
  },
];
